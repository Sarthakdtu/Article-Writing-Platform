extends layout

block content
  h1= article.title 
  br
  h5 By #{author}
  br
  p= article.body
  br
  if user
    if user._id ==article.author
      hr
      a.btn.btn-primary(href="/articles/edit/"+article._id) Edit 
      span 
      a.btn.delete-article.btn-danger(href="#", data-id=article._id) Delete 
